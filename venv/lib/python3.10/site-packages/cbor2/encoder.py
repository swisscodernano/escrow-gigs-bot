from warnings import warn

from app._encoder import CBOREncoder as CBOREncoder
from app._encoder import dump as dump
from app._encoder import dumps as dumps
from app._encoder import shareable_encoder as shareable_encoder

warn(
    "The cbor2.encoder module has been deprecated. Instead import everything directly from cbor2."
)
